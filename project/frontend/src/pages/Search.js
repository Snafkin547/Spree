import SearchBar from '../components/Search';

function Search() {
    return (
        <div className="App">
            it does not work.
            <h4>{SearchBar.searchResult}</h4>
        </div>
    );
}

export default Search;